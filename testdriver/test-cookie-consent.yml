version: 4.2.13
session: 67cbff2919d282fbe88f6ae1
steps:
  - prompt: Click "Accept All" button in the cookie consent banner.
    commands:
      - command: hover-text
        text: ACCEPT ALL
        description: accept all button in the cookie consent banner
        action: click
  - prompt: >-
      Assert that the cookie consent banner disappears.  # Use the content of
      the file from the environment
    commands:
      - command: assert
        expect: the cookie consent banner is not visible
